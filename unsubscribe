<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>Unsubscribe Status</title>
    
    <style>
        /* A simple CSS reset */
        html, body {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
        }

        body {
            /* Use Flexbox to center the content */
            display: flex;
            justify-content: center;
            align-items: center;

            /* Set a clean background and font */
            background-color: #f0f2f5;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            text-align: center;
        }

        h1 {
            color: #333;
            padding: 0 20px; /* Add padding for very narrow screens */

            /* * This makes the font size responsive.
             * It will try to be 10% of the viewport width (10vw),
             * but won't go smaller than 2.5rem or larger than 6rem.
             * This ensures it looks great on all devices.
            */
            font-size: clamp(2.5rem, 10vw, 6rem);
            font-weight: 700;
        }
    </style>
</head>
<body>

    <h1>You may not unsubscribe</h1>

</body>
</html>
